<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>신규 게시글 작성 웹페이지</title>
    <style>
        textarea {
            width: 500px;
        }
    </style>
</head>
<body>
    <h1>신규 게시글 작성</h1>

    <!-- http://localhost:3000/article/create 주소로 post 방식으로 form 태그 내 사용자 입력/선택 폼데이터를 전송합니다. -->
     <!-- submit은 클라이언트에서 서버로 데이터를 전송한다는 의미이고  submit버튼이 클릭되면 서버로 데이터가 전송되기 바로전에 form 태그의 onSubmit이벤트가 발생합니다. -->
     <form method="post" action="/article/create">
        제목:<input type="text" id="title" name="title" /><br/>
        내용:<br/>
        <textarea name="contents" id="contents" rows="20"></textarea><br/>
        게시여부:
        <select name="display" id="display">
            <option value="9">선택</option>
            <option value="1">게시함</option>
            <option value="2">게시안함</option>
        </select>
        <br/>
        <input type="submit" value="저장">
     </form>

     <script>
        function checkForm() {
            if(document.getElementById("title").value === "") {
                alert("{게시글 제목을 입력해주세요}");
                document.getElementById("title").focus();
                return false;
            }

            return true;
        }
     </script>
</body>
</html>